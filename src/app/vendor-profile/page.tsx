import { VendorDemoProfile } from "@/components/VendorDemoProfile";

export const metadata = {
  title: "Your Vendor Profile — MarketFolk",
};

export default function VendorProfileDemoPage() {
  return <VendorDemoProfile />;
}
